.TH CLICKTEST 1
.SH NAME
clicktest \- does a scripted click work a widget?
.SH SYNOPSIS
.B clicktest
[
.B -m
]
[
.B -i
.I item
]
[
.B -e
.I what
.B -n
.I count
]
[
.B -q
]
[
.B -w
.I wherefile
]
[
.B -r
.I resultfile
]
[
.B -t
.I seconds
]
.SH DESCRIPTION
.B Clicktest
puts a button on the screen, writes where the button actually is to
.I wherefile
.RB ( /clicktest.where
by default), and records what reaches it in
.I resultfile
.RB ( /clicktest.result ).
A script reads the position, clicks there, and reads the result.
.PP
It exists because delivering a click and working a widget are different
claims.
.IR Inputtest (1)
shows that a click written to
.B /dev/pointerin
comes back off
.B /dev/pointer
with the right coordinates and times; everything between \(em the pointer
queue,
.IR wm (1),
Tk's event dispatch, the widget's own hit testing \(em is untested by that.
.SH EXAMPLE
.IP
.EX
% load session
% clicktest -t 30 &
% sleep 6
% xy=`{cat /clicktest.where}
% session click $xy 1
% cat /clicktest.result
press 1
.EE
.SS "As a check"
With
.B -e
and
.B -n
it says whether the presses it wanted arrived, in its exit status, so a script
can fail.
Without them it only reports, which is what it was built to do.
.SH "WHAT IT ESTABLISHED"
That a scripted click does work a real Tk button, reliably: twenty-five of
twenty-five presses over five runs.
.PP
That is worth stating plainly because this tree carries the opposite warning
about the neighbouring case.
Synthetic clicks posted at the host with
.B CGEvent
do
.I not
work Tk buttons on this platform, and fail silently.
.B /dev/pointerin
goes in underneath the host, so it is not the same path and does not have that
problem.
.PP
It establishes the whole path as well, not just the pieces.
A script of pointer actions, compiled into a
.I recording
and then played back whole with
.B session play
\(em rather than by calling
.B session click
directly \(em presses the button three times out of three, five runs out of
five.
That is
.IR session (2)
end to end: script, recording, script again, and a widget at the end of it.
.PP
It also found two real defects.
A press and a release written back to back arrive microseconds apart however
they are stamped, and a widget then sees a button that was never really held:
thirteen presses out of fifteen, losing one click in five or so.
.IR Session (2)
now spaces the delivery to match the times the events claim.
Nothing but a widget would have shown that \(em at the device the events are
all present and correctly stamped either way.
.PP
And that a widget which moves between clicks looks exactly like clicks being
dropped.
This program's own label changed width when it recorded a press,
.B pack
re-centred what was beside it, and the button walked out from under the next
click: two clicks in five went missing, and only the repeats, never the first.
The label has a fixed width now.
.PP
That one is worth remembering by anybody scripting a window rather than by
anybody working on
.IR session (2),
because the coordinates in a script are absolute and nothing revisits them.
.SS "The menu"
.B -m
puts up a menu instead, and
.B -i
chooses which of its three items to aim at.
A menu is the case a click does not cover: it posts on the press and tracks the
pointer, so an item is taken by moving onto it and releasing there, which is a
drag rather than a click.
.PP
The file then holds exactly the four numbers a drag wants, so a script needs no
arithmetic:
.IP
.EX
% clicktest -m -i 2 &
% session drag `{cat /clicktest.where} 1
% cat /clicktest.result
two 1
.EE
.PP
Aimed at each item in turn it selects that item and no other:
.IP
.EX
aimed at item 1 -> one
aimed at item 2 -> two
aimed at item 3 -> three
.EE
.PP
Three targets and three different answers is the part that matters.
One of them alone would show only that a menu can be made to do something; that
the answer follows where the pointer went is what shows the movement is being
tracked.
.PP
Getting there needed the menu measured rather than assumed.
Guessing that it sits flush under its button with items the button's height put
the release on the first item while aiming at the second \(em which looks
exactly like a menu ignoring the pointer, and is not one.
It is posted, read, and unposted to find out.
.SH SOURCE
.B /appl/cmd/clicktest.b
.SH SEE ALSO
.IR session (2),
.IR sh-session (1),
.IR inputtest (1),
.IR pointer (3)
.PP
.B /doc/session.ms
\(em
.IR "Recording and Replaying an Inferno Session" ,
a tutorial covering all of this and how it was checked.
.SH BUGS
It reports the button's position from Tk's own
.B -actx
and
.BR -acty ,
added to the toplevel's.
If a window manager moves the window afterwards the position is stale, and the
click goes wherever it now points.
.PP
A button and a menu, which are two kinds of widget out of many.
.PP
What the menu establishes is that the movement inside a
.I drag
is tracked.
A standalone
.B move
\(em the pointer going somewhere and staying there, with no button \(em still
has no demonstration, only the same reasoning.
